const add = (a, b) => a + b;
const sub = (a, b) => a - b;
const mul = (a, b) => a * b;

// module.exports.add = add;
// module.exports.sub = sub;
// module.exports.mul = mul;

module.exports = {
    add: add,
    sub: sub,
    mul: mul,
};
